{
  "name": "trip-model",
  "version": "0.0.0",
  "description": "",
  "main": "./src/index.tsx",
  "scripts": {
    "copy:package": "mkdir -p dist && cp package.json ./src/index.d.ts './dist/'",
    "build": "npm run copy:package && tsc",
    "watch": "npm run copy:package &&  tsc --watch",
    "verify": "eslint --max-warnings=0 --ext .js,.jsx,.ts,.tsx 'src/'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kenny Chen",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "@types/express": "^4.0.0",
    "@types/jest": "^24.0.5",
    "@types/node": "^11.9.4",
    "@typescript-eslint/eslint-plugin": "^1.4.2",
    "@typescript-eslint/parser": "^1.4.2",
    "text-encoding": "^0.7.0",
    "typescript": "^3.3.3",
    "typescript-eslint-parser": "^22.0.0"
  }
}
